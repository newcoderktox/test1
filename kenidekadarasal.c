#include <stdio.h>
int main(){
    int num;
    printf ("Lütfen bir sayı giriniz: \n. Çımak için #");
    scanf ("%c", &num);
    
    do{
        if (num<=0){
        printf ("Geçersin işlem: ")
        }
    }
}